export type EntityRow = {
  name: string;
  bucket: string;
  path: string;
  size: string;
};

export type ActivityItem = {
  id: string;
  title: string;
  description: string;
  timestamp: string;
};

