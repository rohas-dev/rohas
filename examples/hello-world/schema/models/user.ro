model User {
  id        Int      @id @auto
  name      String
  email     String   @unique
  posts     Post[]      // One-to-Many: User has many Posts
  profile   Profile?    // One-to-One: User has one Profile (virtual field)
  deletedAt DateTime?
  createdAt DateTime @default(now)
  updatedAt DateTime @default(now)
}
